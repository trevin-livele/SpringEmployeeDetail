spring:
  datasource:
    username:
    password:
    url: jdbc:mysql://localhost:3606/employeesdata
    driver-class.name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties.hibernate.dialect: org.hibernate.dialect.MySQL5Dialect